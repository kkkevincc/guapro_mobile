# 版本管理指南

## 📚 当前状态
- ✅ 已初始化Git仓库
- ✅ 已创建第一个版本：v1.0 (b357b2a)
- ✅ 包含所有当前功能：梅花易数、UI优化、用户系统等

## 🔄 版本管理常用命令

### 查看版本历史
```bash
# 查看提交历史
git log --oneline

# 查看详细历史
git log --graph --oneline --all
```

### 创建新版本（在进行大修改前）
```bash
# 方法1：创建带标签的版本
git add .
git commit -m "预发布版本：描述此次修改内容"
git tag v1.1-pre

# 方法2：直接提交修改
git add .
git commit -m "v1.1: 新功能或重大修改描述"
```

### 回退到之前版本
```bash
# 查看所有可回退的版本
git log --oneline

# 方式1：切换到特定提交（会进入分离头指针状态）
git checkout b357b2a

# 方式2：回退到标签
git checkout v1.0

# 方式3：完全回退到之前版本（危险操作）
git reset --hard v1.0
```

### 创建分支进行大修改
```bash
# 创建新分支
git branch feature/new-ui
git checkout feature/new-ui

# 或者一步完成
git checkout -b feature/new-ui

# 完成修改后合并到主分支
git checkout main
git merge feature/new-ui

# 删除已合并的分支
git branch -d feature/new-ui
```

## 🚀 推荐的工作流程

### 1. 进行大修改前的步骤
```bash
# 确保当前在主分支
git checkout main

# 确保工作区干净
git status

# 创建备份标签
git tag backup-before-big-change

# 开始修改...
# 进行各种修改...

# 修改完成后提交
git add .
git commit -m "v1.1: 重大UI重构"
```

### 2. 如果修改出现问题
```bash
# 回退到修改前的状态
git checkout backup-before-big-change

# 或者硬重置到备份版本
git reset --hard backup-before-big-change
```

### 3. 保持版本记录
```bash
# 定期创建重要里程碑标签
git tag v1.0-ui-fixed
git tag v1.0-with-梅花易数
git tag v1.0-stable-release
```

## 📋 当前可用的版本
- `b357b2a` - v1.0: 完整功能版本（推荐回退点）
- 可以通过 `git tag v1.0 b357b2a` 为当前版本添加更友好的标签

## ⚠️ 注意事项
1. 每次进行大修改前都创建备份标签
2. 重要功能完成后及时创建里程碑标签
3. 回退操作前确认当前工作状态
4. 分支是进行实验性修改的好方法

## 🔧 实用技巧
```bash
# 查看当前状态
git status

# 查看差异
git diff

# 查看特定文件的历史
git log --oneline js/app.js

# 恢复单个文件到之前版本
git checkout HEAD~1 -- js/app.js
```