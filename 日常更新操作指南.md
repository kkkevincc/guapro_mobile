# 日常更新操作指南

## 🔄 本地更改同步到GitHub的完整流程

### ⚠️ 重要提醒
**GitHub不会自动同步您本地的更改！** 每次修改代码后，您都需要手动执行推送操作。

## 📋 标准操作流程

### 第1步：检查文件状态
```bash
cd /Users/zhenkunchen/Documents/trae_projects/gua_project
git status
```
**目的**: 查看哪些文件被修改了

### 第2步：添加修改的文件
```bash
# 添加单个文件
git add 文件名.js

# 或添加所有修改的文件
git add .
```

### 第3步：提交更改
```bash
git commit -m "你的修改描述"
```
**示例**:
```bash
git commit -m "fix: 修复用户头像显示问题"
git commit -m "feat: 添加新的占卜场景选择"
git commit -m "docs: 更新使用说明文档"
```

### 第4步：推送到GitHub
```bash
git push origin main
```

## 🚀 一键更新命令（推荐）

将以上步骤合并为一个便捷命令：

```bash
cd /Users/zhenkunchen/Documents/trae_projects/gua_project
git add . && git commit -m "update: $(date +'%Y-%m-%d %H:%M:%S')" && git push origin main
```

## 📝 提交信息规范

### 推荐格式
```
类型: 简短描述

详细说明（可选）
```

### 常用类型
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 样式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具相关

### 示例
```
feat: 添加梅花易数时间预测功能
fix: 修复历史记录排序问题
docs: 更新iOS使用指南
style: 优化移动端界面样式
```

## 🔍 状态检查命令

### 查看提交历史
```bash
git log --oneline -10
```

### 查看远程仓库状态
```bash
git remote -v
```

### 查看分支状态
```bash
git branch -a
```

## ⚡ 快捷操作

### 快速推送
```bash
git push origin main
```

### 推送所有分支
```bash
git push origin --all
```

### 推送标签
```bash
git push origin --tags
```

## 🛠️ 常见问题解决

### 问题1：推送被拒绝
**错误**: `Updates were rejected because the remote contains work that you do not have locally`

**解决**: 
```bash
git pull origin main
git push origin main
```

### 问题2：SSH连接失败
**解决**:
```bash
ssh -T git@github.com
```

### 问题3：合并冲突
**解决**:
```bash
git status  # 查看冲突文件
# 手动解决冲突
git add 冲突文件名
git commit -m "resolve: 解决合并冲突"
git push origin main
```

## 📱 实际应用场景

### 场景1：修改了一个功能
1. 修改 `index.html` 或 `js/app.js`
2. 测试确认功能正常
3. 推送：`git add . && git commit -m "feat: 功能改进" && git push origin main`

### 场景2：更新了文档
1. 修改了 `README.md`
2. 推送：`git add README.md && git commit -m "docs: 更新说明文档" && git push origin main`

### 场景3：添加了测试文件
1. 创建了新的测试页面
2. 推送：`git add test.html && git commit -m "test: 添加功能测试" && git push origin main`

## 🔒 安全提醒

- **定期推送**: 建议每次重要修改后都立即推送
- **版本标签**: 重要版本可以添加标签：`git tag v1.0.1 && git push origin v1.0.1`
- **备份重要**: 本地代码不会自动同步到GitHub，请定期推送

## ✅ 验证同步成功

推送完成后，可以访问：https://github.com/kkkevincc/little-kun

查看：
1. 提交历史中是否出现了新的提交
2. 文件列表和内容是否已更新
3. README.md 是否显示最新的修改

---

**总结**: 本地修改 → git add → git commit → git push → GitHub同步完成 🚀